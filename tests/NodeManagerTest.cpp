#include <gtest/gtest.h>
#include "../src/NodeManager.hpp"


TEST(NodeManagerTest, test)
{
  // NodeManager nodMan(1); 
  
  // NeutralArray *seq = nodMan.allocate(),
  //   *seq2 = nodMan.allocate();

  // ASSERT_TRUE(seq != nullptr);
  // ASSERT_TRUE(seq2 != nullptr);

  // nodMan.deallocate(seq); 
  // nodMan.deallocate(seq2); 

  // seq = nodMan.allocate();
  // seq2 = nodMan.allocate();
  // NeutralArray *seq3 = nodMan.allocate(),
  //   *seq4 = nodMan.allocate(),
  //   *seq5 = nodMan.allocate();
  
  // ASSERT_TRUE(seq != nullptr); 
  // ASSERT_TRUE(seq2 != nullptr); 
  // ASSERT_TRUE(seq3 != nullptr); 
  // ASSERT_TRUE(seq4 != nullptr); 
  // ASSERT_TRUE(seq5 != nullptr); 

}
